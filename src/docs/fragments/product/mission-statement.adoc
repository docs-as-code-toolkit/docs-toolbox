Package the documentation build tools used by Docs-as-Code pipelines into one
versioned container image so teams can reduce local setup effort and CI drift.
Keep the image publishing-toolchain-friendly rather than tied to one renderer:
it provides the runtime layer for local and CI-based rendering.
